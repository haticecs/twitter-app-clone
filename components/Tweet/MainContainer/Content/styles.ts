import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  contentContainer: {
    marginVertical: 5,
  },
  content: {
    lineHeight: 18,
  },
  image: {
    width: '100%',
    height: 200,
    resizeMode: 'cover',
    overflow: 'hidden',
    borderRadius: 10,
    marginTop: 5,
  },
})

export default styles
